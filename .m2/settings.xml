<settings>
    <servers>
        <server>
            <id>${repo.id}</id>
            <username>${repo.username}</username>
            <password>${repo.password}</password>
        </server>
        <server>
            <id>${GPG_ID}</id>
            <passphrase>${GPG_KEY}</passphrase>
        </server>
    </servers>
    <profiles>
        <profile>
            <id>ossrh</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <gpg.keyname>gpg2</gpg.keyname>
                <gpg.passphrase>${GPG_KEY}</gpg.passphrase>
            </properties>
        </profile>
    </profiles>
</settings>
