<settings>
    <servers>
        <server>
            <id>${repo.id}</id>
            <username>${repo.username}</username>
            <password>${repo.password}</password>
        </server>
        <server>
            <id>${gpg.GPG_ID}</id>
            <passphrase>${gpg.GPG_KEY}</passphrase>
        </server>
    </servers>
    <profiles>
        <profile>
            <id>${repo.id}</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <gpg.keyname>${gpg.GPG_ID}</gpg.keyname>
                <gpg.passphrase>${gpg.GPG_PASSP}</gpg.passphrase>
            </properties>
        </profile>
    </profiles>
</settings>
